<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.gatech.seclass.jobcompare6300">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JobCompare6300.NoActionBar">


        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.JobCompare6300.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".CurrentJobActivity" />
        <activity
            android:name=".AddNewOfferActivity" />

        <activity
            android:name=".AdjustWeightsActivity" />
        <activity
            android:name=".JobComparisonActivity" />
        <activity
            android:name=".JobRankingActivity" />

    </application>

</manifest>